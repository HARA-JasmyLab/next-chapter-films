{
  "name": "next-chapter-films",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": { "build": "node scripts/build.mjs", "check": "node --check scripts/build.mjs && node --check src/consultation.js" },
  "engines": { "node": ">=22" }
}
